services:

  db:
    image: postgres:13-alpine
    restart: always
    shm_size: 128mb
    ports:
      - '5432:5432'

    volumes:
      - ./data:/var/lib/postgresql/data
    environment:

      POSTGRES_DB: db
      POSTGRES_USER: login
      POSTGRES_PASSWORD: 9mREsvXDs9Gk89Ef

